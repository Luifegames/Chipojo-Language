# Publishing

Users do not publish directly to the chpm repository or database.

Send this package folder name to a chpm admin:

```text
@revaydev/random
```

The admin reviews the project and uploads it to the official chpm repository/database.
